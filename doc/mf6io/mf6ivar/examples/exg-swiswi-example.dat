# The exchange is listed in the EXCHANGES block of the simulation name file
# with the freshwater model first and the saltwater model second, as in
#
#   SWI6-SWI6  island.swiswi  freshwater  saltwater
#
# Both models use the same grid, so no exchange data are required.

BEGIN OPTIONS
  PRINT_INPUT
  PRINT_FLOWS
END OPTIONS
